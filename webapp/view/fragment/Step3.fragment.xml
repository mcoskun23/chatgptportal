<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:form="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified"
>
    <WizardStep
        title="{i18n>List}"
        icon="sap-icon://multi-select"
    >
        <Table items="{model>/List}">
            <headerToolbar>
                <OverflowToolbar>
                    <ToolbarSpacer />
                    <Button
                        text="{i18n>AddRow}"
                        icon="sap-icon://sys-add"
                        press="onPressAddParam"
                    />
                </OverflowToolbar>
            </headerToolbar>
            <columns>
                <Column
                    minScreenWidth="Tablet"
                    demandPopin="true"
                >
                    <Text text="{i18n>Header}" />
                </Column>
                <Column
                    minScreenWidth="Tablet"
                    demandPopin="true"
                >
                    <Text text="{i18n>Type}" />
                </Column>
                <Column
                    minScreenWidth="Tablet"
                    demandPopin="true"
                >
                    <Text text="{i18n>Formule}" />
                </Column>
                <Column hAlign="End" />
            </columns>
            <items>
                <ColumnListItem vAlign="Middle">
                    <cells>
                        <Input value="{model>Header}" />
                        <Select selectedKey="{model>Type}">
                            <core:Item
                                key="01"
                                text="Sorgu"
                            />
                        </Select>
                        <Input value="{model>Formule}" />
                        <Button
                            icon="sap-icon://delete"
                            press="onPressDeleteParam"
                            type="Reject"
                        />
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </WizardStep>
</core:FragmentDefinition>
